# Probes congelados

Cada rodada deve executar: testes de UI e integração, replay determinístico, captura em três resoluções, auditoria de APIs de ordem e benchmark do pipeline. O conjunto só cresce; um probe aprovado não é removido em rodadas posteriores.
